server status {
    listen {
        type    = status
        ipaddr  = *
        port    = 18121
    }
    $-INCLUDE /etc/pint/config/status
    authorize {
        ok

        Autz-Type Status-Server {
            ok
        }
    }
}
